App\Domain\DataInteractor\DTO\Workout\ReferenceWorkoutDTO:
    base_reference_workout (template):
        createdDate : "<(new DateTime())>"
        updatedDate : "<(new DateTime())>"

    reference_workout_ares (extends base_reference_workout):
        name: "ares"
        canonicalName: "ares"
        referenceExercises: ["@reference_exercise_biceps_curl", "@reference_exercise_bench_press", "@reference_exercise_kettle_swing"]

    reference_workout_hera (extends base_reference_workout):
        name: "hera"
        canonicalName: "hera"
        referenceExercises: ["@reference_exercise_sit_up", "@reference_exercise_push_up"]