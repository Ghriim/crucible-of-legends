App\Domain\DataInteractor\DTO\Workout\CategoryDTO:
    base_workout_category (template):
        createdDate : "<(new DateTime())>"
        updatedDate : "<(new DateTime())>"

    workout_category_{fitness, running, biking, boxing} (extends base_workout_category):
        title: '<current()>'