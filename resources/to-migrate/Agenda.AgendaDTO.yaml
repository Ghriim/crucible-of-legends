App\Domain\DataInteractor\DTO\Agenda\AgendaDTO:
    base_agenda (template):
        createdDate : "<(new DateTime())>"
        updatedDate : "<(new DateTime())>"

    agenda_ghriim_default (extends base_agenda):
        player: "@user_ghriim"
        isDefault: true

    agenda_ghriim_triathlon (extends base_agenda):
        name: 'triathlon'
        player: "@user_ghriim"

    agenda_charo_default (extends base_agenda):
        player: "@user_charo"